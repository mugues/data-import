CREATE TABLE BUSINESS_PARTNER (id int IDENTITY(1,1) NOT NULL, note varchar(MAX) NOT NULL)
CREATE TABLE STORICO_JSON (id_bp int NOT NULL, data_inserimento varchar(100) NOT NULL, messaggio varchar(MAX) NOT NULL)
